class Solution {
public:
    /*
        Brute force :
            -> first sort intervals;
            -> Now traverse to all the elements in intervals & maintain a 
            -> maintain 
    */
    vector<vector<int>> merge(vector<vector<int>>& intervals) {
        vector<vector<int>> ans;
    }
};